function glossaryEsc(v){return escapeHtml(v||'').replace(/\n/g,'<br>')}
function glossaryCsvIds(value){return String(value||'').split(',').map(v=>v.trim()).filter(Boolean)}
function glossaryRelatedCard(kind,row){const href=`../${kind}/${row.url_id}.html`;if(kind==='works')return entityWorkCard(row,href);if(kind==='characters')return entityCharacterCard(row,href);if(kind==='songs')return entitySongCard(row,href);return entityGlossaryCard(row,href)}
function renderGlossaryRelatedDropdowns(g,works,characters,songs,glossary,lang){const root=document.getElementById('glossary-related-tabs');if(!root)return;root.innerHTML='';const defs=[['works',works,glossaryCsvIds(g.related_work_ids),{ja:'関連作品',en:'Related Works'}],['characters',characters,glossaryCsvIds(g.related_character_ids),{ja:'関連キャラクター',en:'Related Characters'}],['songs',songs,glossaryCsvIds(g.related_song_ids),{ja:'関連楽曲',en:'Related Songs'}],['glossary',glossary,glossaryCsvIds(g.related_glossary_ids),{ja:'関連用語',en:'Related Terms'}]];const available=defs.map(([kind,data,ids,label])=>[kind,data.filter(r=>ids.includes(String(r.work_id||r.character_id||r.song_id||r.term_id))),label]).filter(([,data])=>data.length);if(!available.length)return;available.forEach(([kind,data,label])=>{const details=document.createElement('details');details.className='glossary-related-group';const summary=document.createElement('summary');summary.innerHTML=`<span class="glossary-related-group-title">${lang==='en'?label.en:label.ja}</span><span class="glossary-related-group-count">${data.length}</span>`;details.appendChild(summary);const list=document.createElement('div');list.className='glossary-detail-related';list.innerHTML=data.map(row=>glossaryRelatedCard(kind,row)).join('');details.appendChild(list);root.appendChild(details)})}
function glossaryCurrentLanguage(){return typeof getLanguage==='function'&&getLanguage()==='en'?'en':'ja'}
function glossaryContentBlocks(value){if(Array.isArray(value))return value.filter(b=>b&&typeof b==='object').map(b=>({align:b.align==='right'?'right':'left',text:String(b.text??'')}));if(typeof value==='string'&&value.trim())return [{align:'left',text:value}];return []}

async function renderGlossaryDetail(){
  const id=document.body.dataset.glossaryId;
  const [glossary,works,characters,songs,data]=await Promise.all([loadCSV('../data/glossary.csv'),loadCSV('../data/works.csv'),loadCSV('../data/characters.csv'),loadCSV('../data/songs.csv'),fetch(`../data/glossary/${id}.json`,{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('Glossary data could not be loaded.');return r.json()})]);
  const g=glossary.find(x=>x.term_id===id);if(!g)throw new Error('Glossary entry not found');
  const lang=glossaryCurrentLanguage(),en=lang==='en';document.documentElement.lang=lang;
  const title=en?(g.term_en||g.term_ja):(g.term_ja||g.term_en);document.title=`${title} - Kagamito Official`;
  document.getElementById('glossary-name').textContent=title;document.getElementById('glossary-secondary').textContent=en?(g.term_ja||''):(g.reading_ja||'');
  renderGlossaryRelatedDropdowns(g,works,characters,songs,glossary,lang);
  const root=document.getElementById('glossary-yaml-detail');root.innerHTML='';
  const sections=Array.isArray(data.sections)?data.sections:[];
  sections.forEach(section=>{const heading=en?section.title_en:section.title_ja;const blocks=glossaryContentBlocks(en?section.content_en:section.content_ja);if(!heading&&blocks.every(b=>!b.text.trim()))return;const el=document.createElement('section');el.className='glossary-detail-section';if(heading){const h=document.createElement('h2');h.className='glossary-detail-heading';h.textContent=heading;el.appendChild(h)}blocks.forEach(b=>{if(!b.text.trim())return;const p=document.createElement('div');p.className='glossary-detail-text';p.style.textAlign=b.align;p.innerHTML=glossaryEsc(b.text);el.appendChild(p)});if(el.children.length)root.appendChild(el)});
  const rendered=root.querySelectorAll('.glossary-detail-section');rendered.forEach((section,index)=>{if(index<rendered.length-1){const divider=document.createElement('div');divider.className='glossary-detail-section-divider';divider.setAttribute('aria-hidden','true');section.after(divider)}})
}

document.addEventListener('DOMContentLoaded',()=>renderGlossaryDetail().catch(e=>{console.error(e);const root=document.getElementById('glossary-yaml-detail');if(root)root.innerHTML=`<p class="glossary-detail-empty">${escapeHtml(e.message)}</p>`}));
